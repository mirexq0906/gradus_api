import React from 'react';
import RequisitesInfo from '../containers/requisites/requisitesInfo';

const Requisites = () => {
    return (
        <main className="requisites-page">
            <RequisitesInfo/>
        </main>
    );
};

export default Requisites;