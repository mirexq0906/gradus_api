import React from 'react';
import DeliveryInfo from '../containers/delivery/deliveryInfo';

const Delivery = () => {
    return (
        <main className="delivery-page">
            <DeliveryInfo/>
        </main>
    );
};

export default Delivery;