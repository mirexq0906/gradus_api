import React from 'react';
import ContactsInfo from '../containers/contacts/contactsInfo';

const Contacts = () => {
    return (
        <main className="contacts-page">
            <ContactsInfo/>
        </main>
    );
};

export default Contacts;